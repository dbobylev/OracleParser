/*ZZZ*/CREATE OR REPLACE PACKAGE BODY TEST_PKG IS

CMYCONST NUMBER := 100;

/* COMMENT */
PROCEDURE OUT_SCREEN(TOSC IN VARCHAR2)
IS
BEGIN
 DBMS_OUTPUT.ENABLE;
 DBMS_OUTPUT.PUT_LINE(TOSC);
END OUT_SCREEN;

FUNCTION MIN_TWO_NUM(A IN FFF.ZZZ, B IN GRO.FFF.ZZZ) RETURN NUMBER
IS
  g numner;
BEGIN
 CMYCONST := 333333;
 CMYCONST34 := 5555555;
 LL.CMYCONST35 := 6666666;
 ll.runproc;
 g := factorial(4);
 g5 := ais.magfc.factorial99(489988888888888888888888);
 RETURN (A - B);
END MIN_TWO_NUM;

FUNCTION ADD_TWO_NUM(A IN NUMBER, B /*ERGREGERG*/       IN NUMBER) RETURN NUMBER
IS
q number;
sup date;
fff signtype;
kkk QQQQQQ;
BEGIN
select b.q into q from shit b where b.dfdf = sdfsd(b.f44f);
 RETURN (A + B);
END             ADD_TWO_NUM;

FUNCTION FACTORIAL(NUM IN NUMBER) RETURN NUMBER
IS
BEGIN
	IF (NUM <=1) THEN
	 RETURN (NUM);
	ELSE
	 RETURN (NUM * FACTORIAL(NUM-1));
	END IF;
END FACTORIAL;

END TEST_PKG;
/